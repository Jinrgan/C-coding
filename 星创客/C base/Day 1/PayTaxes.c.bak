#include <stdio.h>

void main()
{
	int money = 0, tax = 0;
	
	scanf ("%d", &money);
	
	money *= 0.93;
	printf("%d\t%d\n", money, money - 3500);
	
	if((money - 3500 > 0) && (money - 3500 <= 1500))
		tax = (money-3500) * 0.03;
	else if((money - 3500 > 1500) && (money - 3500 <= 4500))
		tax = (money-3500) * 0.10;
	else if((money - 3500 > 4500) && (money - 3500 <= 9000))
		tax = (money-3500) * 0.20;
	else if((money - 3500 > 9000) && (money - 3500 <= 35000))
		tax = (money-3500) * 0.25;
	else if((money - 3500 > 35000) && (money - 3500 <= 55000))
		tax = (money-3500) * 0.30;
	else if((money - 3500 > 55000) && (money - 3500 <= 80000))
		tax = (money-3500) * 0.35;
	else if((money - 3500 > 80000))
		tax = (money-3500) * 0.45;
		
	printf("%d\n", tax);
}